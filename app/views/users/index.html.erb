<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Users</title>
  </head>
  <body>
    <h1>All Users</h1>
    <ul>
      <% @users.each do |user| %>
        <li><%= link_to user.full_name, user_path(user) %></li>
      <% end %>
    </ul>
  </body>
</html>
