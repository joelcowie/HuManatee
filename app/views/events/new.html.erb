<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Event</title>
  </head>
  <body>
    <div class="row">
    </div>
    <div class="row">
      <div class="col s12">
          <div class="content title">
            <h1>Create An Event</h1>
          </div>
      </div>
    </div>
    <div class="row">
      <div class="col s12">
          <div class="content">
            <%= form_for @event do |f| %>
            <%= f.label :title %><br>
            <%= f.text_field :title %><br><br>
            <%= f.label :description %><br>
            <%= f.text_area :description %><br><br>
            <div class="row">
              <%= f.label "Start date and army time" %>
            </div>
            <div class="row">
              <%= f.datetime_select :start_date, {}, {:ampm => true, :minute_step => 15, class: 'browser-default col s4 date'} %>
            </div>
            <div class="row">
              <%= f.label "End date and army time" %>
            </div>
            <div class="row">
              <%= f.datetime_select :end_date, {}, {:ampm => true, :minute_step => 15, class: 'browser-default col s4'}%>
            </div>
            <%= f.submit %>
            <% end %>
          </div>
      </div>
    </div>
  </body>
</html>
